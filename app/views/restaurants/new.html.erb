<div class="container">
  <h1 class ="m-5">New Restaurant</h1>
  <%= simple_form_for @restaurant do |f| %>
    <%= f.input :name %>
    <%= f.input :address %>
    <%= f.input :phone_number %>
    <%= f.input :category, as: :select, collection: Restaurant::CATEGORIES, prompt: "Seleccione una categoría" %>
    <%= f.submit "Crear Restaurante", class: "btn btn-primary" %>
  <% end %>
</div>
